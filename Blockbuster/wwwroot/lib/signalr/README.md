# Vendored SignalR browser client

`signalr.min.js` is copied from `@microsoft/signalr` 10.0.11 (MIT License),
published by Microsoft. Its source package and lockfile are pinned in the
repository's `package.json` and `package-lock.json`. This local asset avoids a
runtime CDN dependency.
